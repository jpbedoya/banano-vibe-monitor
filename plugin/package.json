{
  "name": "banano-vibe-plugin",
  "version": "1.0.0",
  "description": "OpenClaw plugin for Banano's two-layer Discord vibe moderation",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "deploy": "npm run build && openclaw plugins install . && openclaw gateway restart",
    "logs": "node scripts/logs-summary.mjs",
    "logs:all": "node scripts/logs-summary.mjs --all",
    "logs:path": "echo ~/.openclaw/extensions/banano-vibe/logs"
  },
  "dependencies": {
    "sentiment": "^5.0.2"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^22.0.0"
  }
}
