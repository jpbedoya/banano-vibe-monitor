{
  "id": "banano-vibe",
  "name": "Banano Vibe Monitor",
  "description": "Two-layer vibe moderation for Discord: local sentiment gate + AI review. Monitors watched channels, redirects bad vibes, escalates to mods.",
  "version": "1.3.0",
  "configSchema": {
    "type": "object",
    "properties": {
      "enabled": {
        "type": "boolean",
        "description": "Enable vibe monitoring (default: true)"
      },
      "watchedChannelIds": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Discord channel IDs to monitor for vibes"
      },
      "modChannelId": {
        "type": "string",
        "description": "Discord channel ID for mod escalations"
      },
      "modRoleIds": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Discord role IDs allowed to use !banano stop/start (empty = check Discord permissions)"
      },
      "modUserIds": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Discord user IDs always allowed to use mod commands"
      },
      "sentimentThreshold": {
        "type": "number",
        "description": "Sentiment score threshold for escalation (default: -2)"
      },
      "maxRecentMessages": {
        "type": "number",
        "description": "Number of recent messages for AI context (default: 10)"
      },
      "cooldownMs": {
        "type": "number",
        "description": "Cooldown per channel between vibe actions in ms (default: 30000)"
      },
      "dedupeWindowMs": {
        "type": "number",
        "description": "Window for message deduplication in ms (default: 60000)"
      },
      "contextFilterBots": {
        "type": "boolean",
        "description": "Filter bot messages from AI context window (default: true)"
      },
      "modEscalationMinSeverity": {
        "type": "string",
        "enum": ["low", "medium", "high"],
        "description": "Minimum severity to escalate to mod channel (default: high)"
      },
      "highSeverityPublicReply": {
        "type": "boolean",
        "description": "Whether high-severity issues also get an in-channel reply (default: true). Set false for silent escalation only."
      },
      "vibeReviewTimeoutMs": {
        "type": "number",
        "description": "Timeout for isolated AI vibe review in ms (default: 30000)"
      }
    }
  }
}
